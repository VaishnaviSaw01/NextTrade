# ============================================================
# Frontend Environment Variables (.env)
# ============================================================
# SECURITY: Only NON-SECRET values belong in this file.
# All secrets (OAuth client_secret, session keys, TOTP keys,
# admin emails) are configured in backend/.env ONLY.
# ============================================================

# Google OAuth2 Configuration (public client ID - safe for frontend)
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here
VITE_OAUTH_REDIRECT_URI=http://localhost:8080/admin/auth/callback

# Session Configuration (non-secret timeout value)
VITE_SESSION_TIMEOUT=1800000

# TOTP Display Configuration
VITE_TOTP_ISSUER=InsightBull

# Security Configuration (UI behaviour flags only)
VITE_RATE_LIMIT_WINDOW=900000
VITE_RATE_LIMIT_MAX_ATTEMPTS=5
VITE_ENABLE_HTTPS=true
VITE_SECURE_COOKIES=true

# Timezone Configuration (OPTIONAL)
# Leave empty to auto-detect user's browser timezone (recommended)
# IANA timezone examples: Asia/Kuala_Lumpur, America/New_York, Europe/London, UTC
# VITE_USER_TIMEZONE=

# API Configuration
VITE_API_BASE_URL=http://127.0.0.1:8000